class Solution(object):
    def shuffle(self, nums, n):
        arr = []
        i = 0
        j = n

        while(j <len(nums)):
            arr.append(nums[i])
            arr.append(nums[j])
            i+=1
            j+=1

        return arr    
